name: Refresh Letterboxd data

on:
  schedule:
    - cron: "17 9 * * *" # daily; odd minute to avoid the on-the-hour rush
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Fetch most recent watches
        run: node scripts/fetch-activity.mjs

      # Only the films fetch-activity just added are missing a poster, so this
      # is a handful of requests a day once the catalogue is filled in.
      - name: Fetch posters for new films
        run: node scripts/fetch-posters.mjs

      # Both counts are on the card, so they refresh together on one commit.
      - name: Fetch follower counts
        run: node scripts/fetch-followers.mjs

      - name: Commit if changed
        id: commit
        run: |
          if git diff --quiet src/data/people.json src/data/activity.json src/data/films.json; then
            echo "No changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/data/people.json src/data/activity.json src/data/films.json
          git commit -m "Refresh Letterboxd activity and follower data"
          git push
          echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Tell IndexNow the pages changed
        if: steps.commit.outputs.changed == 'true'
        run: node scripts/submit-indexnow.mjs
